import { montserrat, press } from '@/app/ui/fonts';

export default function Carreira() {
  return (
    <main>
        <h1 className={`${press.className} font-sunrise`}>Carreira Page</h1>
    </main>
  )
}
